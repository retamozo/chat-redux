import {}
const initialState = {
    messageId: 
}

const messages = (state = initialState, action) => {
switch(action.payload){
    case 'ADD_MESSAGE':
        return{

        }
}
    
}